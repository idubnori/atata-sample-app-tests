variables:
  buildConfiguration: 'Debug'
  atata.config.alias: 'ci'

jobs:
- job: 'Build_Tests'
  pool:
    name: Hosted VS2017
  steps:
  - task: DotNetCoreInstaller@0
    displayName: Install .NET Core sdk 2.2
    inputs:
      version: 2.2.100

  - task: DotNetCoreCLI@2
    displayName: Build
    inputs:
      command: build
      arguments: '--configuration $(buildConfiguration)'
      projects: '**/*.csproj'

  - task: DotNetCoreCLI@2
    displayName: Test
    inputs:
      command: test
      projects: '**/*Tests.csproj'
      arguments: '--no-build'